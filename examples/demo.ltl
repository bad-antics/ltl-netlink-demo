// examples/demo.ltl — runnable demo for ltl_netlink_demo

import std::io
use ltl_netlink_demo as lib

fn main() {
    io::println("ltl_netlink_demo demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}